version: '3.9'
services:
  nodejs:
    image: node:latest
    env_file:
      - stack.env
    restart: always
    deploy:
      replicas: 1
    ports:
      - "8888:8888"
    working_dir: /app
    entrypoint: sh -c "rm -rf gfapi-website && git clone https://github.com/kejonaMC/gfapi-website.git && cd gfapi-website && npm install && npm run build && npm start"
