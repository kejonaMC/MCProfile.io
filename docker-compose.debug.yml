version: '3.4'

services:
  xuidapi:
    image: xuidapi
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 8888:8888
    command: ["node", "--inspect=0.0.0.0:9229", "app.js"]
